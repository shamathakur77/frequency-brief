{
  "name": "frequency-brief",
  "private": true,
  "description": "Deterministic daily almanac card renderer",
  "dependencies": {
    "@fontsource/atkinson-hyperlegible": "^5.0.0",
    "playwright": "^1.47.0"
  },
  "scripts": {
    "card": "python3 -m frequency_brief && node frequency_brief/screenshot_card.js"
  }
}
